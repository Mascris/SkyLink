spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=SkyLinkdb;encrypt=true;trustServerCertificate=true
    username: sa
    password: amineSERRAR52479.
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.SQLServerDialect

server:
  port: 8080
